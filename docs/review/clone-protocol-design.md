# clone プロトコル設計書(F5 `cloneWiki` / F7 `openRepository`)

**この文書はレビュー専用です。実装 PR にもマージ先ブランチにも含めません。**

対象ブランチ: `gitlab-ls-9.3.0` / 関連 issue: #14(D15)/ 前サイクルの設計書: Close 済み Draft PR #70

## 1. 背景と目的

D15 の 8 コマンドのうち 6 つ(F1〜F4 / F6 / F8)は前サイクルで設計・実装を終えた
(PR #71 #72 #73 #76 #77 #78)。残る **F5 `cloneWiki`** と **F7 `openRepository`** は、
前サイクルの Codex レビュー 7 巡を通じて **「clone 先の所有権・確定・異常終了からの回復」プロトコル**が
繰り返し指摘の中心になり、方式を 3 度変えても
**「自分が作ったものだけを消す」ことを保証できない**という同じ壁に当たったため、
ユーザー判断で独立サイクルへ切り出された(前設計 §4.1)。

本設計の目的は、**その壁を回避する方針を確定させたうえで** F5 / F7 を実装可能な形にすることである。

### 1.1 前サイクルで未解決だった指摘(本設計の入力)

| 指摘 | 内容 |
|---|---|
| 第 3 巡 R3-7 | `CloneCommand` の内部作成物を呼び出し側から識別できない |
| 第 5 巡 R5-2 | `ATOMIC_MOVE` は宛先存在時の置換可否が実装依存(Linux は空ディレクトリを置換する) |
| 第 6 巡 R6-1 | `createDirectory` はディレクトリの作成者を保証するだけで、子要素の所有者は保証しない |
| 第 7 巡 R7-1 | **名前の秘匿は所有権の根拠にならない**(ディレクトリは列挙・監視から発見できる) |
| 第 7 巡 R7-2 / R7-3 | ステージング昇格時の同名衝突、予約と永続記録の間のクラッシュ窓 |

### 1.2 本設計の中心的な決定(2026-08-15・ユーザー判断)

> **D1: 失敗しても、このプラグインのコードは一切削除しない。**

上記 5 件の指摘はすべて **「作った物を安全に消す」** ための機構に関するものである。
**消さないと決めれば、機構ごと不要になる。** 代償は「失敗時にディスク上に途中経過が残る」ことだが、
**データ損失は発生しない**(消えるものが無い)。これは既存機能の方針
(F6 §12「作成したプロジェクトも remote も削除しない」/ F4「外部の変更を上書きするくらいなら
中間状態のまま残す」)とも一貫する。

**この決定は本設計の全体を規定する。以降の節はすべてこの前提の上に立つ。**

**ただし JGit 自身は削除する**(第 4 巡の検証で判明・実バイナリで確認)。
`CloneCommand.cleanup()` は失敗経路で次を行う:

- `directoryExistsInitially` が false → **`FileUtils.delete(directory)` で宛先ごと削除**
- true → **`deleteChildren(directory)` で中身を削除**

**D2 により宛先は「存在しない」か「空」のいずれかなので、ユーザーのデータが失われることはない**
(JGit が消すのは JGit 自身が作ったものか、空ディレクトリの中身)。
**したがって D1 は「このプラグインのコードが削除しない」という意味であり、
「ディスク上に必ず何かが残る」という意味ではない。**§13.2 (b) の文面はこれを踏まえる。

**`cleanup()` は `call()` が失敗を返す経路でしか走らない。** JVM や Eclipse が
**異常終了した場合は走らない**ため、そのときだけ中途半端な clone が残る(§13.2 (b) / §9.4-5)。

## 2. 対象範囲

- **F5 `cloneWiki`**: 現在のリポジトリに対応する GitLab Wiki を clone し、ワークスペースへ取り込む。
- **F7 `openRepository`**: GitLab プロジェクトを指定して clone し、ワークスペースへ取り込む。

前設計 §4 の決定を引き継ぐ: VSCode の `gl.openRepository` は仮想 FS(`gitlab-remote://`)で
**clone せずに**開くが、Eclipse に相当機能が無いため **「選んで clone して取り込む」に再定義**する。
**VSCode との挙動差が生じる**(ディスク消費・初回時間)。台帳 #7 に注記する。

## 3. 対象外(意図的に実装しない)

- **仮想ファイルシステム**(`gitlab-remote://` 相当)。Eclipse に基盤が無く、本サイクルの範囲を超える。
- **失敗した clone の自動削除・ロールバック**(D1)。
- **プロジェクト種別の推定インポート**(m2e / buildship の呼び出し)。**新規依存を作らないため**(D3)。
- **SSH での clone**(D4)。
- **submodule の取得**、**部分 clone / shallow clone**、**特定ブランチの指定**。
- **clone 済みリポジトリの更新(pull)**。

## 4. 要件

### 4.1 機能要件

| ID | 要件 |
|---|---|
| R1 | F5: アクティブファイルから GitLab プロジェクトを解決し、その **Wiki の clone URL を導出**して clone する |
| R2 | F7: **プロジェクトパスの入力**で対象を指定し、存在を検証したうえで clone する |
| R3 | clone 先は **親ディレクトリ + フォルダ名**をユーザーが指定する |
| R4 | clone 後、可能ならワークスペースへ取り込む(D3) |
| R5 | 進捗を表示し、**キャンセルできる** |
| R6 | 失敗時は **何も削除せず**、残っている可能性のある場所を伝える |

### 4.2 非機能要件

- **新規 OSGi 依存を追加しない。** `build.gradle.kts` / `detekt.yml` の差分ゼロ。
- ディレクトリ構成を変えない(既存 `com.gitlab.eclipse.*` 体系内に追加)。
- `plugin.xml` は追加のみ。
- **ログに URI・パス・トークンを出さない**(既存 A9)。**通知のパスは §13.2 の例外規定に従う。**
- 行長 120 文字以内。

## 5. 前提条件と制約

- **同梱 JGit は 7.5.0**(`org.eclipse.jgit-7.5.0.202512021534-r.jar`)。
- **EGit UI (`org.eclipse.egit.ui`) には依存していない**(ビルドの p2 リポジトリに EGit は入っているが、
  依存として取っているのは `org.eclipse.jgit` / `org.eclipse.jgit.ssh.apache` / `org.apache.sshd.osgi` のみ)。
  **したがって EGit のクローンウィザードへの委譲は「必須依存の追加」を意味し、本設計では採らない。**
- 認証は既存 `GitAuthConfigurer` のみを使う。トークンを URL に埋めない。
- headless の devcontainer では **ワークベンチ依存の動作(取り込み)を検証できない**。手動検証に回す。

## 6. 決定事項(ユーザー判断・2026-08-15)

| ID | 決定 | 根拠 |
|---|---|---|
| **D1** | **失敗しても一切自動削除しない** | §1.2 |
| **D2** | **clone 開始前に「宛先が存在して非空なら開始しない」だけを判定する**(第 1 巡 R1-1 で改訂) | **失敗段階を確実に区別するため。**これは所有権の判定ではなく入口の分岐であり、§1.1 の壁とは別物 |
| **D3** | **`.project` があればインポート、無ければフォルダ名で単純プロジェクトを作る** | m2e / buildship への依存を作らない |
| **D4** | **HTTPS 固定** | API に到達できているなら必ず通る。SSH は鍵・エージェント・`known_hosts` に左右され、headless で検証できない失敗モードが増える |
| **D5** | **`ProgressMonitor` アダプタ + `setTimeout`** を実装する | 前設計 §14-1 が要求し、F6(PR #77)で見送った宿題。clone はこれが最も要る場面 |

**D2 は当初「事前判定をせず JGit に任せる」だったが、第 1 巡 R1-1 で改訂した。** 判定しないままだと、
既存の非空ディレクトリを指定した場合も clone 途中の失敗も**同じ失敗経路**になり、
§13.2 が所有権を主張できない以上、復旧手順が「プラグインが何も作っていないディレクトリの削除」を
ユーザーに指示しうる。**それは D1 が回避したはずの所有権問題を手動操作へ移すだけで、
データ損失に直結する。** 入口で分岐すれば、失敗段階を確実に区別できる(§13.2)。

## 7. システム構成

```
[UI] CloneWikiHandler / OpenRepositoryHandler
        │  (SWT: 入力・確認・通知。UI スレッド)
        ▼
[背景] CloneTargetLookup       … F5/F7 共通。プロジェクト → 存在検証 + http_url_to_repo
[背景] WikiUrlDeriver          … F5 のみ。http_url_to_repo → wiki URL
        │
        ▼
[背景] RepositoryCloner ──uses──▶ EclipseProgressMonitorAdapter
        │                         GitAuthConfigurer(既存)
        ▼
[UI/背景] ClonedProjectImporter … ワークスペースへ取り込む
```

## 8. コンポーネントの責務

| コンポーネント | 責務 | 依存 | 試験 |
|---|---|---|---|
| `EclipseProgressMonitorAdapter` | JGit `ProgressMonitor` を実装し Eclipse `IProgressMonitor` へ委譲。`isCancelled()` が Eclipse 側のキャンセルを返す | なし(純ロジック) | **単体可** |
| `WikiUrlDeriver` | 末尾スラッシュを落とし、**末尾の `.git` があれば除いて** `.wiki.git` を付ける | なし | **単体可** |
| `CloneTargetLookup` | プロジェクト → 存在検証 + `http_url_to_repo`。**F5 と F7 の両方が使う** | `GitLabApiClient` | **単体可**(MockK) |
| `RepositoryCloner` | `CloneCommand` の実行。auth / monitor / timeout を設定。**削除経路を持たない** | JGit / `GitAuthConfigurer` | **単体可**(`file://` の実リポジトリから実 clone) |
| `ClonedProjectImporter` | clone 先をワークスペースへ取り込む | Eclipse Resources | **headless 不可** → 手動検証 |
| ハンドラ 2 本 | 入力・確認・通知・スレッド境界 | SWT | **headless 不可** → 手動検証 |

## 9. 処理フロー

### 9.1 `cloneWiki`(F5)

1. **[UI]** アクティブエディタのファイルを取得。無ければ通知して終了。
2. **[背景]** `GitLabProjectUrlResolver.resolveContextForFile` でプロジェクトを解決。
   解決できなければ通知して終了。
3. **[背景]** **常に API の `http_url_to_repo` から** wiki URL を導出する(第 1 巡 R1-5 で確定)。
   `CloneTargetLookup` で解決済みプロジェクトの `http_url_to_repo` を取得し、
   `WikiUrlDeriver` に渡す。**リポジトリの remote URL は「どのプロジェクトか」の解決にだけ使い、
   URL の組み立てには使わない。**
   - **D4 で clone を HTTPS 固定と決めた以上、remote が SSH か HTTPS かに関わらず
     clone に使う URL は同じ**であり、remote を分岐の材料にする理由が無い。
   - **SSH remote しか無いリポジトリでも F5 は動く。** 中止しない。
   - API が失敗した場合は §13.1 の定型文で中止する(clone は開始しない)。
4. **[UI]** clone 先を入力させる(親ディレクトリ + フォルダ名)。**キャンセルで終了、副作用ゼロ**。
4a. **[背景/UI]** **孤立した登録の回復を、入口判定より前に行う**(第 4 巡 R4-1 の帰結)。
   **宛先に対応する閉じたプロジェクトがワークスペースに残っていないか**を見る
   (§9.4-5 の「閉じている」かつ「location が宛先と一致」)。あれば**同意を求めて登録だけ解除する**。
   **これを (a0) より前・入口判定より前に置くことで、回復可能性が clone の完了状態に依存しなくなる。**
4b. **[背景]** **入口判定**(D2)。宛先が存在して非空なら:
   - **同一 URL の完了済み clone なら clone を省略し、6 の取り込みへ飛ぶ**(§13.2 (a0) / R3-1)。
   - そうでなければ **§13.2 (a) で中止**(削除は案内しない)。
5. **[UI→背景]** `IProgressMonitor` 付きの Job で `RepositoryCloner.clone` を実行。
6. **[背景/UI]** 成功なら `ClonedProjectImporter` で取り込み。
7. **[UI]** 結果を通知。

### 9.2 `openRepository`(F7)

1. **[UI]** プロジェクトパスを入力させる(U3 の方針を踏襲。検索ピッカーは作らない)。
2. **[背景]** `CloneTargetLookup` で存在検証し、`http_url_to_repo` を得る。
   見つからなければ通知して終了。
3. 以降は §9.1 の 4〜7 と同一。

### 9.3 clone の実行(共通)

1. `CloneCommand` に `setURI` / `setDirectory` / `setProgressMonitor(adapter)` /
   `setTimeout(TRANSPORT_TIMEOUT_SECONDS)` を設定し、`GitAuthConfigurer.applyAuth` を通す。
2. `call()` の結果は `Git` を返すので **必ず `close()` する**(ハンドルを開いたままにしない)。
3. **例外・キャンセルのいずれでも、こちらからは何も削除しない**(D1)。

### 9.4 取り込み(共通・D3)

1. clone 先に **`.project` があれば**: `IWorkspace.loadProjectDescription` で読み、
   `IProject.create(description)` → `open()`。
   **同名プロジェクトが既にワークスペースにある場合は取り込みを行わず、その旨を通知する**
   (既存プロジェクトを勝手に差し替えない)。
2. **`.project` が無ければ**: フォルダ名でプロジェクトを作り、`IProjectDescription.setLocation`
   で clone 先を指す。**名前が衝突する場合は取り込まない**(上と同じ)。
3. **取り込みの失敗は clone の失敗ではない。** clone は成功しているので、
   「clone は完了したが取り込めなかった。手動でインポートしてください」と通知する。
4. **`create()` が成功して `open()` が失敗した場合の補償**(第 1 巡 R1-3 で追加)。
   不正な nature/builder、リソース障害、ワークスペースエラーで `open()` は失敗しうる。
   放置すると**閉じた `IProject` がワークスペースに残り**、次回は 1/2 の同名衝突で拒否されるため、
   「手動でインポート」の案内でも復旧できない。したがって:
   - **`IProject.delete(deleteContent = false, force = true, monitor)` で
     ワークスペース登録だけを解除する。** `deleteContent = false` なので **clone の中身は消えない** —
     D1(ディスクの内容を消さない)と両立する。消すのは**直前にこのフローが作った登録だけ**であり、
     **所有権は明確**(§1.1 の問題とは異なる)。
   - **この補償自体が失敗した場合**は、**ディスク内容を残す操作を具体的に示す**(第 2 巡 R2-3)。
     **「削除してください」とだけ案内してはならない** — Eclipse の削除ダイアログには
     **「Delete project contents on disk」のチェックボックス**があり、
     単に Delete を案内すると**ユーザーが clone 全体を失う経路が残る**。
     文面は手順を明示する:

     > 「ワークスペースに閉じたプロジェクト `<名前>` が残っています。
     > プロジェクトを右クリック → **Delete** →
     > **「Delete project contents on disk」のチェックを外したまま** OK してください。
     > **チェックを入れると clone した内容も消えます。**」

     **ディスク上の clone には触れない。**
   - **`create()` 自体が失敗した場合は登録が作られていない**ので、補償は不要。
5. **`create()` 後・`open()` 前に Eclipse が異常終了した場合の回復**(第 2 巡 R2-4)。
   補償(4)は `open()` の失敗を**捕捉できた場合**しか働かない。プロセスが落ちれば閉じた `IProject` が
   残り、**再起動後は「このフローの所有物だ」と判定する永続情報が無い**ため、
   次回は 1/2 の同名衝突で拒否され、**取り込みが恒久的に塞がれる**。
   **永続記録は導入しない**(§12)。代わりに、**取り込み時に観測できる事実**で回復する:
   - 同名のプロジェクトが**閉じており**、かつ**その location が今回の clone 先と一致する**場合、
     **前回の中断で残った登録である可能性が高い**と判断する。
     **ただし自動では解除しない** — ユーザーが自分で作った可能性を排除できないため。
   - **明示的な同意を求める**:
     「同名のプロジェクト `<名前>` がありますが、閉じており、場所は今回の clone 先と同じです。
     前回の中断で残ったものと思われます。**登録だけ解除して取り込み直しますか?**
     **ディスク上の内容は消えません。**」
   - 同意があれば `delete(deleteContent = false)` してから 1/2 を再試行する。
     **同意が無ければ取り込まず**、§9.4-4 と同じ手順(チェックを外して Delete)を案内する。
   - **location が一致しない同名プロジェクトは、この経路に入れない。** 別物なので触らない。

## 10. API / インターフェース

### 10.1 REST

| 用途 | メソッド / パス | 使う欄 |
|---|---|---|
| プロジェクトの存在検証 + clone URL | `GET /projects/{namespace%2Fpath}` | `http_url_to_repo` |

**参照実装からの意図的な逸脱(第 1 巡 R1-4)**: 参照実装は `url.replace(/\.git$/, '.wiki.git')` だが、
これは **remote が `.git` で終わらない場合に入力をそのまま返す**。GitLab は `https://…/group/project`
形式の remote を受理し、既存 `GitLabRemoteParser.parseGitLabRemote` も `.git` を任意として扱うため、
この入力は現在の解決経路から排除されていない。**その場合 F5 は Wiki ではなく本体リポジトリを clone し、
成功したように見える。** したがって「末尾の `.git` は**任意**として除去する」規則に変える。
§9.1-3 により入力は API 由来の正規 URL になるが、**防御的にこの規則を保つ**。

**既存 `ProjectDetailService.getProject` は `GitLabProject(id, default_branch)` しか返さない。**
`http_url_to_repo` を **nullable フィールドとして `GitLabProject` に加算的に追加**する
(既存の利用箇所は影響を受けない)。**新規サービスを作るか既存を拡張するかは実装計画で確定する。**

### 10.2 定数(実ソースで確定済み)

| 定数 | 値 | 出典 |
|---|---|---|
| wiki URL 変換 | **末尾スラッシュを落とし、末尾の `.git` を「あれば」除いて `.wiki.git` を付ける** | 参照実装 `gitlab_remote_source.ts:35-37` から**意図的に逸脱**(下記) |
| transport タイムアウト | **30 秒**(`setTimeout` は秒単位) | 前設計 U6 |

### 10.3 主要インターフェース(案)

```kotlin
sealed interface CloneOutcome {
  data class Cloned(val directory: File) : CloneOutcome
  /** 取り込みまで完了。 */
  data class Imported(val directory: File, val projectName: String) : CloneOutcome
  /** clone は成功したが取り込めなかった。clone 結果は有効。 */
  data class ImportSkipped(val directory: File, val reason: ImportSkipReason) : CloneOutcome
  data object Cancelled : CloneOutcome
  /** [type] は例外の型名のみ。メッセージは載せない(§13.1)。 */
  data class Failed(val type: String, val directory: File) : CloneOutcome
}

enum class ImportSkipReason { NAME_TAKEN, NO_WORKSPACE, IMPORT_FAILED }
```

## 11. データモデル

**永続化する状態を持たない。** 前サイクルで問題になった「予約」「永続記録」は D1 により不要になった。
これは本設計の重要な性質であり、**R7-3(予約と永続記録の間のクラッシュ窓)が構造的に消える**。

## 12. トランザクション境界

**ディスク上の内容については、戻す操作自体を持たない。** 状態は次の 3 つのいずれかで、
**いずれもディスク上に観測できる**:

| 状態 | 観測 | 扱い |
|---|---|---|
| clone 先が存在しない | ディレクトリ無し | 何も起きていない |
| clone 先に途中経過がある | ディレクトリ有り・`.git` が不完全(**`HEAD` が解決できない**) | **このプラグインは残す**(D1)。ただし `call()` が失敗を返した経路では **JGit の `cleanup()` が既に消していることが多い**(§1.2)。§13.2 (b) は**存在を確認してから**文面を選ぶ |
| clone 完了 | 正常なリポジトリ | 取り込みへ進む |

**ワークスペース登録は別の軸であり、こちらは戻す**(第 1 巡 R1-3)。ディスクの内容と違って
**所有権が明確**(直前にこのフローが `create` したもの)であり、放置すると次回の取り込みを
恒久的に塞ぐため:

| 状態 | 観測 | 扱い |
|---|---|---|
| 登録なし | ワークスペースに無い | 何も起きていない |
| **`create` 済み・`open` 失敗** | 閉じた `IProject` が残る | **`delete(deleteContent=false)` で登録だけ解除**(§9.4-4) |
| 解除も失敗 | 閉じた `IProject` が残る | **通知して手順を案内**(§9.4-4)。ディスクには触れない |
| **`create` 後・`open` 前に Eclipse が異常終了** | 閉じた `IProject` が残る | **§9.4-5 の回復経路**(第 2 巡 R2-4) |
| 取り込み完了 | 開いた `IProject` | 正常 |

**異常終了で残った登録を「永続記録」で追跡はしない**(第 2 巡 R2-4 への対応方針)。
所有権を永続化すると、**前サイクルで D1 が消したはずの「予約と永続記録の間のクラッシュ窓」(R7-3)が
そのまま戻ってくる**。代わりに **§9.4-5 で、取り込み時に観測できる事実から回復する**。

## 13. エラー処理

### 13.1 JGit のメッセージを素通ししない

JGit の例外メッセージは **リモート URL を含みうる**(前設計 §13)。
ユーザー向けは定型文、ログは**例外の型名のみ**とする。

> 例: 「clone できませんでした。リポジトリへアクセスできない可能性があります。」

**非空の宛先はこの経路に入らない**(第 2 巡 R2-1)。改訂後の D2 は clone 開始前に非空を検出し、
**§13.2 (a) の専用通知へ分岐する**。したがって **§13.1 の定型文に「空でない可能性」を含めない** —
含めると、実装者がこの節に従って**所有権不明の宛先を §13.2 (b) の削除候補として扱いうる**。
**入口判定は一意である。**

### 13.2 残物の通知(A9 の明示的な例外)

**失敗した段階によって文面を変える**(第 1 巡 R1-1)。D2 の入口判定があるため、この 2 つは確実に区別できる。

**(a0) 宛先が非空だが、そこに「今回 clone しようとしたのと同じリポジトリの完了済み clone」がある場合**
(第 3 巡 R3-1)— **clone を省略して §9.4 の取り込みへ進む。**

`create()` 後・`open()` 前の異常終了から**同じ clone 先で再実行**すると、そこには完了済みの clone が
あるため、これが無いと (a) で終了し、**§9.4-5 の回復経路に永久に到達しない**
(別の空の場所を選んでも location が一致しないので同じく到達しない)。**恒久的な取り込み阻害が残る。**

**識別は所有権ではなく同一性で行う。** 次の**3 つすべて**を満たす場合のみ (a0) に入る:

1. 宛先が**有効な git リポジトリ**である。
2. **`origin` の URL が今回 clone しようとした URL と一致する。**
3. **`HEAD` が実在のコミットへ解決できる**(第 4 巡 R4-1)。

**3 が無いと、中断された clone を「完了済み」と誤認する。** 同梱 JGit 7.5.0 の `CloneCommand` は
`fetch()` の内部で **`RemoteConfig.update()` → `StoredConfig.save()` を実 fetch の前に実行する**
(実バイナリで確認)。したがって認証失敗・タイムアウト・キャンセル・異常終了で fetch が中断されても
**1 と 2 は成立してしまう**。完了した clone は checkout まで済んでいるので `HEAD` が解決でき、
中断された clone は ref も object も無いので解決できない。**この差が唯一の確実な signal である**
(完了マーカーのような永続記録は §11 の方針により持たない)。

**何も削除せず、何も上書きしない。既にあるものを取り込むだけである。**
満たさない場合(壊れた `.git`、別リポジトリ、git ですらない、fetch 中断)は (a) へ落とす。

**空のリポジトリを clone した場合は 3 を満たさない**ため (a0) に入らない。これは受容する —
**登録の回復は (a0) に依存していない**(§9.1-4a)ので、恒久的な取り込み阻害にはならない。

**(a) 宛先が非空で、(a0) にも当たらない場合** — **プラグインは何も作っていない。**

> 「指定した場所は空ではないため、clone を行いませんでした。**何も変更していません。**
> **別の空の場所を指定して**やり直してください。」

**削除を一切促さない。** ここでディレクトリの削除を案内すると、**プラグインが何も作っていない
既存データをユーザーが消しうる**(第 1 巡 R1-1)。**D1 が回避したはずの所有権問題を
手動操作へ移すことになる**ため、この経路では「別の場所」だけを案内する。

**(b) clone を開始したあとに失敗・キャンセルした場合**

**JGit の `cleanup()` が走ったかどうかで、残るものが変わる**(§1.2)。**通知の前に宛先の存在を確認し、
文面を選ぶ**:

> (残っていない場合)「clone は完了しませんでした。**指定した場所には何も残っていません。**
> やり直すか、別の場所を指定してください。」

> (残っている場合)「clone は完了しませんでした。**このプラグインは何も削除していません。**
> `<clone 先>` に途中経過が残っています。**中身を確認したうえで**削除するか、
> 別の場所でやり直してください。」

**「必ず残る」と書かない。** `call()` が失敗を返した経路では JGit 自身が消していることが多く、
残るのは主に**異常終了した場合**である(§1.2)。

- **削除を「指示」しない。「中身を確認したうえで」判断させる。**
  入口判定により、この経路の宛先は**開始時点で存在しないか空だった**ので、
  中身は今回のものである蓋然性が高い。それでも**外部プロセスが同時に書いた可能性は排除できない**ため、
  断定はしない。
- **パスを通知に含めるのは、A9 の趣旨に対する意図的な例外である。**
  A9 が守るのは「ユーザーが与えていない情報の漏洩防止」であり、
  **この clone 先はユーザー自身がその場で入力した値**であるため、返して見せることは漏洩に当たらない。
  **ログには出さない**(ログは持ち出されうるため、型名のみ)。

## 14. タイムアウト・リトライ・冪等性

- **タイムアウト**: `TransportCommand.setTimeout(30)`(§10.2)。
  **progress callback に到達しない状態(経路が無い・接続がブラックホール)は
  `isCancelled()` では抜けられないため、タイムアウトが最後の砦になる。**
- **キャンセル**: `EclipseProgressMonitorAdapter.isCancelled()` が Eclipse の `IProgressMonitor` を返す。
  **Eclipse の Job が monitor を持つだけでは JGit のブロッキング transport は止まらない**ため、
  アダプタを必ず `setProgressMonitor` で渡す(前設計 §14-1)。
- **リトライ**: **自動リトライしない。** 失敗を提示し、再実行の判断をユーザーに委ねる。
- **冪等性**: **clone 自体は非冪等**だが、**同じ場所への再実行は入口判定で分岐する**
  (第 3 巡 R3-3。改訂前の D2 の記述をここに残していた)。JGit に委ねない:
  - **同一 URL の完了済み clone がある** → **clone を省略して取り込みへ進む**(§13.2 (a0))。
    **結果として、同じ操作の再実行は取り込みまで含めて安定した結果になる。**
  - **それ以外の非空** → **入口判定で中止する**(§13.2 (a))。**削除は案内しない。**

## 15. 並行処理

- **clone は「正規化した clone 先パス」をキーに直列化する**(第 1 巡 R1-2 で改訂)。
  **当初は「同一 clone 先への同時実行は JGit が拒否する」としていたが、これは排他保証にならない。**
  `CloneCommand` の宛先検査は check-then-act であってロックではなく、2 つの clone が
  **宛先の作成・非空化より前に同時に検査を通過**すると、両方が同じ作業ツリーと `.git` を
  初期化・更新できてしまう。失敗するだけでなく、**破損したリポジトリ**や
  **一方が成功を通知した後に他方が書き換える**状態が生じる。D2 の事前判定も同じ理由で
  排他にはならない(判定と作成の間に窓がある)。
- **既存 `GitOperationGuard` をそのまま流用できる。** 同クラスの `withRepo(key: String)` は
  **キーが任意の文字列**(`ConcurrentHashMap` のキー)であり、`gitDir` である必要はない。
  clone の開始から完了(取り込みの前)まで保持する。**専用の guard を新設しない。**
- **キーは「ファイルシステム上の同一性」で作る**(第 2 巡 R2-2)。
  既存 guard は**文字列の完全一致しか比較しない**ため、`toAbsolutePath().normalize()` 程度では
  **symlink 経由と実体パス**、**大文字小文字を区別しない環境での表記違い**が別キーになり、
  **同じ実ディレクトリへの 2 つの clone が同時に guard を取得して**破損を再発させる。規則:
  1. **存在する最も近い祖先**を `Path.toRealPath()` で解決する
     (clone 先自身は存在しないか空なので、`toRealPath()` が使えるとは限らない)。
  2. 残りの相対セグメントを連結する。
  3. **1 と 2 を連結したキー全体を小文字化する**(第 3 巡 R3-2)。
     **祖先部分だけ元の大小文字を残してはならない。** clone 先 `.../Foo` が未作成なら
     最初の実行のキーは「実在親 + `foo`」だが、その実行が `Foo` を作成した直後
     (**まだ空なので D2 を通る**)に 2 つ目が来ると、最も近い実在祖先は `.../Foo` 自身になり、
     残りセグメントが無いためキーは `Foo` のままになる。**同じ宛先なのに 2 つの guard を取得でき、
     R1-2 の破損が再発する。**
- **小文字化は意図的に「過剰に寄せる」側の選択である。** 大文字小文字を区別するファイルシステムでは
  `Foo` と `foo` は別ディレクトリなので、このキーは**本来別物を同一視しうる**。
  その場合の結果は「同じ場所への clone が実行中」として**中止される**だけであり、
  **過剰な統合は安全側・不足した統合は破損側**なので、前者を選ぶ。
- **同一キーが取れなかった場合は「同じ場所への clone が実行中」として中止する**
  (guard は待たずに `null` を返す既存挙動)。
- **UI スレッド境界**: 入力・確認・通知・取り込みの `IProject` 操作は UI スレッド。
  clone は背景。既存 `WorkspaceProjectPicker` の形(UI で読む → coroutine → `asyncExec` で戻る)を踏襲する。
- 例外がコルーチンから漏れると**共有 plain-Job スコープが道連れで停止する**(issue #16)。
  すべての `launch` で `CancellationException` は再送出、それ以外は封じ込める。

## 16. 認証と認可

- clone は **`GitAuthConfigurer.applyAuth` のみ**を使う。HTTPS + `oauth2:<token>`。
- **トークンを URL に埋めない。ログに出さない。**
- D4 により SSH transport は使わないが、`GitAuthConfigurer` は SSH transport にも
  プラグイン所有の `SshdSessionFactory` を設定する既存挙動を持つ。**この挙動は変更しない。**

## 17. ログ・監視・監査

- ログは**例外の型名と、種別を表す短い語のみ**。URI・パス・トークンを出さない(A9)。
- 通知は §13.2 の規定に従う。

## 18. 障害時の復旧方法

**プラグイン側にディスク内容の復旧経路を持たない**ことが設計上の性質である(D1)。
**ただし「削除して再実行」を唯一の手順として案内してはならない**(第 1 巡 R1-1)。
復旧は失敗した段階によって分かれる:

| 段階 | 状態 | 案内する手順 |
|---|---|---|
| **宛先が非空で開始しなかった** | **何も変更していない** | **別の空の場所を指定して再実行。削除は案内しない** |
| clone 途中で失敗・キャンセル | 途中経過が残る | **中身を確認したうえで**削除するか、別の場所で再実行 |
| `open()` 失敗 | 閉じた `IProject` が残る | 自動で登録解除を試みる(§9.4-4)。失敗したら手順を案内 |

**ワークスペース登録だけは、所有権が明確なので自動で解除する**(§12)。

## 19. 既存機能への影響

- `GitLabProject` に nullable フィールドを 1 つ追加する(§10.1)。**既存の利用箇所は影響を受けない。**
- **`EclipseProgressMonitorAdapter` は clone 専用にせず、`BranchPushService` からも使えるようにするか**は
  **未決事項 U3**(§22)。
- それ以外の既存コードは変更しない。

## 20. 移行方法・ロールバック方法

- 永続状態を持たないため、**移行は不要**。
- ロールバックは PR の revert で完結する。**ユーザーのディスクに残った clone は revert しても残る**が、
  それは通常のディレクトリであり、プラグインが管理しているものではない。

## 21. テスト方針と受け入れ条件

### 21.1 テスト方針

| 対象 | 方法 |
|---|---|
| `EclipseProgressMonitorAdapter` | 純ロジック。Eclipse `IProgressMonitor` をモックし、`isCancelled()` の委譲と `beginTask`/`update` の変換を検証 |
| `WikiUrlDeriver` | 純ロジック |
| `CloneTargetLookup` | MockK で `GitLabApiClient` をスタブ |
| `RepositoryCloner` | **`file://` の実リポジトリから実 clone** して検証(JGit の実挙動が価値であるため、モックしない) |
| `ClonedProjectImporter` / ハンドラ | **headless では試験不能**。手動検証手順を PR 本文に記載 |

### 21.2 受け入れ条件

| ID | 条件 |
|---|---|
| C1 | F5 / F7 が `plugin.xml` に登録され、メニューから起動できる |
| C2 | **clone 先の入力をキャンセルした時点でファイルシステムが不変**である |
| C3 | 正常系: clone が完了し、`.project` があればワークスペースに現れる |
| C4 | **`.project` が無い場合もフォルダ名でプロジェクトが作られる**(D3) |
| C5 | **同名プロジェクトが既にある場合、既存を差し替えず通知する** |
| C6 | **失敗・キャンセルのいずれでもプラグインがディスクの内容を削除しない**(D1) |
| C12 | **宛先が存在して非空のとき、clone を開始せず、通知が削除を促さない**(§13.2 (a) / R1-1)。**既存ファイルが 1 件も変化しない** |
| C13 | **同一 clone 先への同時実行が直列化される**(§15 / R1-2)。**barrier で同時開始を再現**し、一方が中止になること、成功した側のリポジトリが破損しないことを検証する |
| C14 | **`create()` 成功後に `open()` が失敗した場合、ワークスペース登録だけが解除され、clone の中身が残る**(§9.4-4 / R1-3)。解除後に**同名で再取り込みできる** |
| C15 | **SSH remote しか無いリポジトリでも F5 が動く**(§9.1-3 / R1-5) |
| C16 | **§13.1 の定型文が「空でない」に言及しない**(R2-1)。非空の宛先は §13.2 (a) にのみ分岐する |
| C17 | **symlink 経由の宛先と実体パスの宛先が同一キーに統合される**(§15 / R2-2)。大文字小文字だけが異なる指定も同一キーになる |
| C18 | **補償失敗時の通知が「Delete project contents on disk のチェックを外す」ことを明示する**(§9.4-4 / R2-3)。この経路を試験で通す |
| C19 | **閉じた同名プロジェクトの location が clone 先と一致するとき、同意を得て登録だけ解除し、取り込みが成功する**(§9.4-5 / R2-4)。**同意しなければ何も変えない**。**location が異なる同名プロジェクトには触れない** |
| C20 | **`create` 後の異常終了を、同じ clone 先での再実行から回復できる**(§9.1-4a / §13.2 (a0) / R3-1)。**完了済み clone があると clone を省略して取り込みへ進む**。**壊れた `.git` / 別リポジトリの URL / git でない中身は (a0) に入らず (a) へ落ちる**。**孤立登録の回復は (a0) の成否に依存しない**(§9.1-4a) |
| C23 | **fetch が中断された clone(`origin` は設定済みだが `HEAD` が解決できない)を、再実行が (a0) として取り込まない**(R4-1)。**認証失敗・タイムアウト・キャンセルの各中断後に検証する** |
| C24 | **失敗時の通知が、宛先の実在を確認したうえで文面を選ぶ**(§13.2 (b) / §1.2)。**JGit の `cleanup()` が消した場合に「残っています」と言わない** |
| C21 | **未作成の宛先を作成した直後(まだ空)に 2 つ目の clone が来ても同一キーになる**(§15 / R3-2)。**祖先部分の大小文字がキーに残らない** |
| C22 | **§14 の冪等性の記述が入口判定に統一されている**(R3-3)。**JGit の拒否に委ねる記述が規範として残っていない**(否定形の経緯説明は可) |
| C7 | **無応答 transport に対してタイムアウトで終了**し、キャンセルが JGit に伝播する(D5) |
| C8 | **JGit の例外メッセージが通知にもログにも現れない**(§13.1) |
| C9 | F5 が wiki URL を正しく導出する。**境界: `.git` あり / `.git` なし / 末尾スラッシュあり**(R1-4)。**`.git` で終わらない入力で本体リポジトリの URL をそのまま返さないこと** |
| C10 | F7 が存在しないプロジェクトパスに対して clone を開始しない |
| C11 | 新規 OSGi 依存が無く、`build.gradle.kts` / `detekt.yml` の差分が無い |

## 22. 未決事項(推測で確定しない)

| ID | 内容 |
|---|---|
| **U1** | **clone 先の入力 UI**。`DirectoryDialog`(親)+ `InputDialog`(フォルダ名)の 2 段か、単一の入力か。実装計画で確定する |
| ~~U2~~ | **解消**(第 1 巡 R1-5)。**常に API の `http_url_to_repo` から導出する**と確定した(§9.1-3)。番号は再利用しない |
| **U3** | **`EclipseProgressMonitorAdapter` を `BranchPushService` にも適用するか**(PR #77 で見送った §14-1 の宿題を、ここで全面的に返すか、clone に限るか) |
| **U4** | **取り込み時のプロジェクト名衝突**の扱い。通知して中止(本設計案)か、別名を提案するか |
| **U5** | **F7 のプロジェクト指定**を、PR #78 で入れた `SelectedProjectStore` の割当と連携させるか(clone 後に自動割当するか) |

## 23. 想定されるリスク

| リスク | 影響 | 緩和 |
|---|---|---|
| **【受容】失敗した clone がディスクに残る** | ディスク消費・混乱 | **D1 の意図的な帰結。**§13.2 (b) で場所を伝える。所有権は主張しない |
| **ユーザーが案内に従って既存データを削除する** | **データ損失** | **D2 の入口判定で段階を区別**し、**何も作っていない経路では削除を一切案内しない**(§13.2 (a) / C12) |
| **同一 clone 先への同時実行でリポジトリが破損する** | データ破損 | **clone 先パスをキーにした直列化**(§15 / C13)。JGit の宛先検査はロックではない |
| **`open()` 失敗で閉じたプロジェクトが残り、以後の取り込みを恒久的に塞ぐ** | 機能停止 | **登録だけを `deleteContent=false` で解除**(§9.4-4 / C14) |
| **`create` 後・`open` 前の異常終了で登録が残る** | 機能停止 | **永続記録は使わず**、location 一致という観測事実 + 同意で回復(§9.4-5 / C19)。**同じ場所での再実行がその経路に到達できるよう、完了済み clone は clone を省略して取り込みへ進む**(§13.2 (a0) / C20) |
| **未作成→作成済みへの遷移中に別名キーで直列化をすり抜ける** | データ破損 | **キー全体を小文字化**する(§15 / C21) |
| **手動解除の案内どおりに操作して clone 全体を失う** | **データ損失** | Eclipse の削除ダイアログの**チェックボックスを名指しで外させる**(§9.4-4 / C18) |
| **symlink / 大文字小文字の表記違いで直列化をすり抜ける** | データ破損 | キーを**実パス + 小文字化**で作る(§15 / C17)。**過剰統合は安全側**として受容 |
| **`.git` で終わらない remote で本体リポジトリを clone してしまう** | **誤動作(気づけない)** | 常に API 由来の URL を使い(§9.1-3)、導出規則も `.git` を任意扱いにする(§10.2 / C9) |
| **中断された clone を「完了済み」と誤認して不完全な作業ツリーを取り込む** | **誤動作(気づけない)** | (a0) の条件に **`HEAD` が解決できること**を入れる(§13.2 (a0) / C23)。`origin` は fetch 前に保存されるので判定に使えない |
| 大きなリポジトリの clone が長時間 UI を占有する | UI 凍結 | `IProgressMonitor` 付き Job + キャンセル(D5) |
| キャンセルしても JGit transport が止まらない | 機能停止 | アダプタ + `setTimeout`(D5 / C7) |
| 取り込みでワークスペースの既存プロジェクトを壊す | データ損失 | **同名なら取り込まない**(C5) |
| VSCode 版との挙動差(仮想 FS 対 ローカル clone) | 期待との齟齬 | 前設計 §4 の決定どおり。台帳 #7 に注記 |
| JGit の例外メッセージにリモート URL が含まれる | 情報露出 | §13.1 |

## 24. レビューで特に確認してほしい点

1. **D1(何も削除しない)が、前サイクルで未解決だった R3-7 / R5-2 / R6-1 / R7-1 / R7-2 / R7-3 を
   本当にすべて解消しているか。** 消さないことで新たに生じる問題は無いか。
2. **§13.2 のパス通知が A9 の例外として妥当か。** ログとの区別の付け方は十分か。
3. **§15 の「clone を `GitOperationGuard` の対象にしない」判断が妥当か。**
   同一 clone 先への同時実行を JGit の拒否だけに委ねてよいか。
4. **§9.4 の取り込みが、既存ワークスペースを壊しうる経路を残していないか。**
5. **§22 の未決事項が、実装開始前に決めるべきものとして過不足ないか。**

## 25. Codex レビュー反映履歴(第 1 巡)

**指摘 5 件・すべて P1・押し返し 0 件。** いずれも検証のうえ受け入れた。

| ID | 指摘 | 反映 |
|---|---|---|
| **R1-1** | §18 が「残ったディレクトリの削除」を唯一の復旧手順にしているが、D2 により**既存の非空ディレクトリを指定した場合も同じ失敗経路**に入り、§13.2 自身が所有権を主張できない。**プラグインが何も作っていないディレクトリをユーザーが指示どおり削除して既存データを失いうる** — D1 が回避したかった所有権問題を手動操作へ移しただけ | **D2 を部分撤回**(ユーザー判断・2026-08-15)。**入口で「宛先が存在して非空なら開始しない」だけを判定**し、失敗段階を確実に区別する。§13.2 を (a)(b) に分割し、**(a) では削除を一切案内しない**。§18 の復旧手順を段階別の表に置換。**C12 を追加** |
| **R1-2** | §15 の「同一 clone 先への同時実行は JGit が拒否する」は**排他保証にならない**。`CloneCommand` の宛先検査は check-then-act であってロックではなく、2 つが同時に通過すると**破損したリポジトリ**や**成功通知後に他方が書き換える**状態が生じる | **正規化した clone 先パスをキーに直列化**する。**既存 `GitOperationGuard.withRepo(key: String)` はキーが任意の文字列なのでそのまま流用**でき、専用 guard を新設しない。**C13(barrier で同時開始を再現)を追加** |
| **R1-3** | §9.4 で `create()` 成功後に `open()` が失敗すると、**閉じた `IProject` がワークスペースに残り**、次回は同名衝突で拒否されるため「手動でインポート」でも復旧できない。§12 の 3 状態にも収まらない | **`IProject.delete(deleteContent = false)` で登録だけ解除**する補償を §9.4-4 に追加。**中身は消えないので D1 と両立**し、**所有権も明確**(直前に自分が create した)。§12 に**ワークスペース登録の軸**を追加。補償失敗時の通知と手順も規定。**C14 を追加** |
| **R1-4** | `url.replace(/\.git$/, '.wiki.git')` は remote が `.git` で終わらない場合に**入力をそのまま返す**。GitLab は `https://…/group/project` を受理し、既存 `GitLabRemoteParser` も `.git` を任意扱いなので排除されていない。**F5 が Wiki ではなく本体リポジトリを clone し、成功したように見える** | 導出規則を**「末尾スラッシュを落とし、末尾の `.git` が**あれば**除いて `.wiki.git` を付ける」**に変更。**参照実装からの意図的な逸脱**として §10.2 に明記。**C9 に境界(`.git` なし / 末尾スラッシュ)を追加** |
| **R1-5** | §9.1 が「HTTPS remote が無ければ中止」と書きながら同じ記述で API fallback も指定し、U2 でその fallback を未決にしている。**SSH-only remote という明示条件で実装者により結果が変わる** | **常に API の `http_url_to_repo` から導出する**と確定。remote URL は「どのプロジェクトか」の解決にだけ使う。**D4 で clone を HTTPS 固定と決めた以上、remote を分岐材料にする理由が無い。****SSH-only でも F5 は動く。****U2 を解消**(欠番)。`CloneTargetLookup` は F5/F7 共通に格上げ。**C15 を追加** |

### 反映後に残る主要な論点

- **D1(何も自動削除しない)は維持**。R1-1 は D1 ではなく **D2(判定しない)** の問題であり、
  入口判定を戻すことで解消した。**「作った物を消す」機構は依然として持たない。**
- **例外は 1 つだけ: ワークスペース登録の解除**(R1-3)。これは**ディスクの内容を消さず**、
  **所有権が明確**なので、§1.1 の壁とは別問題である。

## 26. Codex レビュー反映履歴(第 2 巡)

**指摘 4 件・すべて P1・押し返し 0 件。**

| ID | 指摘 | 反映 |
|---|---|---|
| **R2-1** | §13.1 が**改訂前の D2 のまま**「事前判定をしない」と書き、非空宛先を JGit の一般失敗経路に入れていた。**実装者がこの節に従うと R1-1 と同じく所有権不明の宛先を §13.2 (b) の削除候補として扱いうる** | **第 1 巡の反映漏れ。**§13.1 から旧記述と「空でない可能性」を削除し、**非空はこの経路に入らない**ことを明記。**入口判定を一意にした**。**C16 追加** |
| **R2-2** | 既存 guard は**文字列の完全一致**しか見ないため、`toAbsolutePath().normalize()` 程度では **symlink 経由と実体パス**、**大文字小文字を区別しない環境の表記違い**が別キーになり、**同じ実ディレクトリへの 2 つの clone が同時に guard を取得**して破損を再発させる | キーを**「存在する最も近い祖先の `toRealPath()` + 残りの相対セグメントを小文字化」**で作る規則に変更。**小文字化は過剰統合側の意図的選択**(過剰統合は中止で済むが、統合不足は破損)。**C17 追加** |
| **R2-3** | 補償失敗時の「プロジェクトエクスプローラから削除してください。中身は消えません」は**安全でない**。Eclipse の削除 UI には**「Delete project contents on disk」の選択肢**があり、**ユーザーが clone 全体を失う経路が残る** | 文面を**具体的な手順**に変更し、**チェックボックスを名指しで「外したまま」**と指示。**「チェックを入れると clone した内容も消えます」**も明記。**C18 追加** |
| **R2-4** | `create()` 後・`open()` または補償の前に **Eclipse が異常終了**した場合も閉じた `IProject` が残る。**再起動後は所有物と判定する永続情報が無く**、次回は同名衝突で拒否され、**R1-3 と同じ恒久的な取り込み阻害**が残る | **永続記録は導入しない**(導入すると D1 が消した R7-3「予約と永続記録の間のクラッシュ窓」が戻る)。代わりに **§9.4-5** で、**「閉じている」かつ「location が clone 先と一致」**という**観測できる事実**から候補を特定し、**明示的な同意を得て登録だけ解除**する。**location が一致しない同名プロジェクトには触れない**。§12 に状態を追加。**C19 追加** |

### 第 2 巡で確認した設計上の性質

- **R2-4 への対応で永続状態を導入しなかった**ことにより、§11「永続化する状態を持たない」は維持されている。
  **前サイクルの R7-3 が戻らないことが、この設計の要である。**
- **R2-1 は第 1 巡の反映漏れだった。** D2 のような横断的な決定を変えるときは、
  **その決定を引用している全節を洗い直す**必要がある(§6 / §9 / §13.1 / §13.2 / §18 / §23)。

## 27. Codex レビュー反映履歴(第 3 巡)

**指摘 3 件・すべて P1・押し返し 0 件。**

| ID | 指摘 | 反映 |
|---|---|---|
| **R3-1** | `create()` 後の異常終了から**同じ clone 先で再実行**すると、そこには完了済み clone があるため **D2 の非空判定で §13.2 (a) に終了し、§9.4-5 の location 比較・同意確認に到達しない**。別の空の場所を選べば location が一致しない。**どちらの経路でも登録を回復できず、C19 を importer 単体で満たしても実際の F5/F7 では恒久的な取り込み阻害が残る** | **§13.2 に (a0) を新設。** 宛先が**有効な git リポジトリ**で **`origin` の URL が今回 clone しようとした URL と一致する**場合、**clone を省略して取り込みへ進む**。**識別は所有権ではなく同一性**で行い、**何も削除せず上書きもしない**。§9.1 の処理フローに 4b として明記。**C20 追加** |
| **R3-2** | キー規則が**祖先部分の大小文字を残す**ため、`.../Foo` が未作成のときのキー(実在親 + `foo`)と、**`Foo` が作成された直後(まだ空で D2 を通る)**のキー(`.../Foo` 自身)が食い違う。**同じ宛先で 2 つの guard を取得でき R1-2 の破損が再発する** | **連結後のキー全体を小文字化する**規則に変更。**祖先部分だけ原形を残してはならない**ことを明記。**C21(未作成→作成済みの遷移中の同時実行)追加** |
| **R3-3** | §14 の冪等性に**改訂前の D2**(「同じ場所への 2 度目は JGit が拒否する」)が残っていた。**この記述どおり JGit に委ねると既存 clone が一般失敗として §13.2 (b) の削除候補になりうる** | §14 を入口判定に統一。**同一 URL の完了済み clone は (a0) で取り込みへ、それ以外の非空は (a) で中止**。**C22 で「JGit の拒否に委ねる記述が規範として残っていないこと」を検証する**(§15 の「当初は…これは排他保証にならない」のような**否定形の経緯説明は残してよい**) |

### 第 3 巡で見えた構造

- **R3-1 は、R1-1(入口判定を戻す)と R2-4(観測事実で回復する)が噛み合って生まれた穴だった。**
  個々の修正は正しくても、**両者が同じ「宛先の状態」を別の目的で読む**ため、
  片方の分岐がもう片方の入口を塞いだ。**横断的な決定の変更は、その決定を読む全経路の
  到達可能性まで確認する必要がある**(§26 の教訓の一段深い形)。
- **R2-1 と R3-3 は同じ種類の反映漏れ**(改訂前の D2 の残骸)。**C22 で機械的に検出できるようにした。**

## 28. Codex レビュー反映履歴(第 4 巡)

**指摘 1 件・P1・押し返し 0 件。** 加えて、**その検証中に設計書の事実誤認を 1 件自己発見して訂正した。**

| ID | 指摘 | 反映 |
|---|---|---|
| **R4-1** | 同梱 JGit 7.5 の `CloneCommand` は `init()` 後の `fetch()` で **`origin` を設定・保存してから実 fetch を開始する**ため、認証失敗・タイムアウト・キャンセルで中断されても**「有効な git リポジトリ + 一致する origin」が成立**する。**(a0) が clone 完了と誤認して不完全な作業ツリーを取り込み、再 clone による復旧にも到達できない** | **実バイナリで確認した**(`RemoteConfig.update()` → `StoredConfig.save()` が実 fetch の前)。**指摘は正しい。**(a0) の条件に **3. `HEAD` が実在のコミットへ解決できる**を追加。**完了した clone は checkout 済みなので解決でき、中断された clone は ref も object も無いので解決できない** — 永続記録を持たない本設計で唯一確実な signal。**C23 追加**。あわせて**孤立登録の回復を入口判定より前(§9.1-4a)へ移し、回復可能性が clone の完了状態に依存しないようにした** |

### 検証中に自己発見した事実誤認(Codex 指摘外)

**D1 の「一切自動削除しない」という記述は、事実として誤りだった。**
`CloneCommand.cleanup()` を実バイナリで確認したところ、失敗経路で:

- `directoryExistsInitially` が false → **`FileUtils.delete(directory)` で宛先ごと削除**
- true → **`deleteChildren(directory)` で中身を削除**

**D2 により宛先は「存在しない」か「空」なので、ユーザーのデータは失われない**が、
**§13.2 (b) の「途中経過が残っています」は多くの場合に偽になる**。訂正:

- **D1 を「このプラグインのコードは削除しない」に限定**(§1.2)。JGit 自身の削除を明記。
- **§13.2 (b) は宛先の実在を確認してから文面を選ぶ**。「必ず残る」と書かない。**C24 追加**。
- §12 のディスク状態表に `cleanup()` の影響を反映。

**教訓**: **外部ライブラリの失敗時の後始末を、設計書が確認せずに前提化していた。**
D1 のような「何が消え、何が残るか」を規定する決定は、**依存ライブラリの実挙動を実バイナリで
確認してから書く**必要がある(前サイクルの「プロトコル定数は実ソースで確定」と同じ原則)。

