<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.gitlab.eclipse</groupId>
    <artifactId>plugin-group</artifactId>
    <version>0.7.3-SNAPSHOT</version>
    <packaging>pom</packaging>

    <modules>
        <module>feature</module>
        <module>update-site</module>
        <module>swtbot</module>
    </modules>

    <properties>
        <tycho.version>4.0.8</tycho.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <!--suppress UnresolvedMavenProperty -->
        <gitlabEclipsePluginProjectId>${env.CI_PROJECT_ID}</gitlabEclipsePluginProjectId>
        <!--suppress UnresolvedMavenProperty -->
        <gitlabGroupId>${env.CI_PROJECT_NAMESPACE_ID}</gitlabGroupId>
    </properties>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.eclipse.tycho</groupId>
                    <artifactId>tycho-p2-director-plugin</artifactId>
                    <version>${tycho.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.eclipse.tycho</groupId>
                <artifactId>tycho-maven-plugin</artifactId>
                <version>${tycho.version}</version>
                <extensions>true</extensions>
            </plugin>
            <plugin>
                <groupId>org.eclipse.tycho</groupId>
                <artifactId>target-platform-configuration</artifactId>
                <version>${tycho.version}</version>
                <configuration>
                    <environments>
                        <environment>
                            <os>win32</os>
                            <ws>win32</ws>
                            <arch>x86_64</arch>
                        </environment>
                        <environment>
                            <os>linux</os>
                            <ws>gtk</ws>
                            <arch>x86_64</arch>
                        </environment>
                        <environment>
                            <os>macosx</os>
                            <ws>cocoa</ws>
                            <arch>x86_64</arch>
                        </environment>
                        <environment>
                            <os>macosx</os>
                            <ws>cocoa</ws>
                            <arch>aarch64</arch>
                        </environment>
                    </environments>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>com.gitlab.eclipse</groupId>
            <artifactId>gitlab-eclipse-plugin</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>com.gitlab.eclipse</groupId>
            <artifactId>gitlab-language-server</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>com.gitlab.eclipse</groupId>
            <artifactId>gitlab-language-server.cocoa.macosx.aarch64</artifactId>
            <version>${project.version}</version>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>com.gitlab.eclipse</groupId>
            <artifactId>gitlab-language-server.cocoa.macosx.x86_64</artifactId>
            <version>${project.version}</version>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>com.gitlab.eclipse</groupId>
            <artifactId>gitlab-language-server.gtk.linux.x86_64</artifactId>
            <version>${project.version}</version>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>com.gitlab.eclipse</groupId>
            <artifactId>gitlab-language-server.win32.win32.x86_64</artifactId>
            <version>${project.version}</version>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <repositories>
        <repository>
            <id>2024-09</id>
            <layout>p2</layout>
            <url>https://download.eclipse.org/releases/2024-09</url>
        </repository>
        <repository>
            <id>gitlab-maven</id>
            <url>https://gitlab.com/api/v4/groups/${gitlabGroupId}/packages/maven</url>
        </repository>
    </repositories>

    <distributionManagement>
        <repository>
            <id>gitlab-maven</id>
            <url>https://gitlab.com/api/v4/projects/${gitlabEclipsePluginProjectId}/packages/maven</url>
        </repository>

        <snapshotRepository>
            <id>gitlab-maven</id>
            <url>https://gitlab.com/api/v4/projects/${gitlabEclipsePluginProjectId}/packages/maven</url>
        </snapshotRepository>
    </distributionManagement>

    <profiles>
        <profile>
            <id>local</id>
            <activation>
                <property>
                    <name>!env.CI</name>
                </property>
            </activation>
            <properties>
                <gitlabEclipsePluginProjectId>gitlab-org%2Feditor-extensions%2Fgitlab-eclipse-plugin</gitlabEclipsePluginProjectId>
                <gitlabGroupId>gitlab-org%2Feditor-extensions</gitlabGroupId>
            </properties>
        </profile>
    </profiles>
</project>
